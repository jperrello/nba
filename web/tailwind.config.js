// Tailwind v4 reads its config primarily from CSS via @theme blocks.
// This file is here so editor tooling and IDE plugins still find a config;
// the source of truth is src/theme/tokens.css.
export default {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
}
